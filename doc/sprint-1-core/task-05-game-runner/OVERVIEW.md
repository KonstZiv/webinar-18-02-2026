# Task 05 — Game Runner

**Спрінт:** 1 — Ядро | **Пріоритет:** High | **Залежить від:** task-03, task-04

Створити `app.py` — точку входу, що збирає `HumanPlayer`, `MinimaxComputerPlayer` та `RichRenderer` у `TicTacToe` engine і запускає ігровий цикл. Оновити `__main__.py` для запуску через `uv run python -m tictactoe_cli`. Тести: `tests/test_app.py`.
