# Task 04 — Обробка введення

**Спрінт:** 1 — Ядро | **Пріоритет:** High | **Залежить від:** task-01 | **Блокує:** task-05

Реалізувати `HumanPlayer` як підклас `Player` — приймає введення у форматі координат `a1..c3`, маппить на індекс клітинки 0-8, повертає `Move`. Модуль `ui/input.py` для парсингу координат. Тести: `tests/test_input.py`.
